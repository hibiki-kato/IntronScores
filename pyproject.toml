[project]
name = "intronscore"
version = "0.1.0"
description = "Plotting scripts for IntronScores"
requires-python = ">=3.8"
dependencies = [
    "matplotlib>=3.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
